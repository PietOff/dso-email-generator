// Importing necessary modules

function generateEmail(name) {
    // Capitalize "AbelTalent"
    const companyName = name.replace(/abelTalent/g, 'AbelTalent');
    // Other email generation logic...
}

module.exports = generateEmail;